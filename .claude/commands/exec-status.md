Report the state of the xFPL executive system. Reading only — change nothing.

From `company/` alone (no repository sweep, no code reading):

1. **Open decisions** — anything in `company/decisions/` awaiting Steven, with what
   each unblocks.
2. **Recent meetings** — the last few from `company/meetings/`, and any recommendation
   that was never decided on.
3. **Blocking UNKNOWNs** — from `company/CONTEXT.md`, the gaps only Steven can fill,
   and what each is holding up.
4. **Context freshness** — when `CONTEXT.md` was last verified, and whether anything in
   it is likely stale.

Keep it to a short briefing. If nothing is outstanding, say so in one line rather than
manufacturing an update.
